/**
 * Branch Protection Test
 * 브랜치 보호 규칙 테스트를 위한 임시 파일
 */

describe('Branch Protection Verification', () => {
  it('should pass this test to verify branch protection works', () => {
    expect(true).toBe(true);
  });

  it('should verify that 1 + 1 equals 2', () => {
    expect(1 + 1).toBe(2);
  });
});
