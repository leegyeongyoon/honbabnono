import React from 'react';
import UniversalWishlistScreen from '../components/shared/UniversalWishlistScreen';

const WishlistScreen = ({ navigation, user }: any) => {
  return <UniversalWishlistScreen navigation={navigation} user={user} />;
};

export default WishlistScreen;