import React from 'react';
import UniversalPointHistoryScreen from '../components/shared/UniversalPointHistoryScreen';

const PointHistoryScreen = ({ navigation, user }: any) => {
  return <UniversalPointHistoryScreen navigation={navigation} user={user} />;
};

export default PointHistoryScreen;