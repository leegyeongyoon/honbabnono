import React from 'react';
import UniversalPrivacySettingsScreen from '../components/shared/UniversalPrivacySettingsScreen';

const PrivacySettingsScreen = ({ navigation, user }: any) => {
  return <UniversalPrivacySettingsScreen navigation={navigation} user={user} />;
};

export default PrivacySettingsScreen;