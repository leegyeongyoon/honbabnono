import React from 'react';
import UniversalReviewManagementScreen from '../components/shared/UniversalReviewManagementScreen';

const ReviewManagementScreen = ({ navigation, user }: any) => {
  return <UniversalReviewManagementScreen navigation={navigation} user={user} />;
};

export default ReviewManagementScreen;